# https://docs.github.com/github/administering-a-repository/configuration-options-for-dependency-updates
version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/" # Location of package manifests
    schedule:
      interval: "monthly"
    groups:
      # Group all GitHub Actions PRs into a single one:
      all-github-actions:
        patterns:
          - "*"
  - package-ecosystem: "julia"
    directories: # Location of Julia projects
      - "/"
      - "/docs"
      - "/test"
    schedule:
      interval: "daily"
    groups:   
      # Groups all julia dep PRs into a single one
      all-julia-packages:
        patterns:
          - "*"
    ignore:
      # No updates for openfhe_julia, since we fix it to one specific version anyways
      - dependency-name: "openfhe_julia_jll"
      - dependency-name: "openfhe_julia_int128_jll"
